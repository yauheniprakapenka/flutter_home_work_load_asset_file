import 'package:flutter/material.dart';

class KitchenTable extends StatelessWidget {
  const KitchenTable({Key? key}) : super(key: key);

  @override
  Widget build(context) {
    return Container(width: 800, height: 80, color: Colors.white);
  }
}
